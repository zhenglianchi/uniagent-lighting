diff --git a/uni_agent/gateway/gateway.py b/uni_agent/gateway/gateway.py
index 0dbcdbc..8e51d41 100644
--- a/uni_agent/gateway/gateway.py
+++ b/uni_agent/gateway/gateway.py
@@ -9,6 +9,7 @@ from __future__ import annotations
 
 import asyncio
 import logging
+import os
 from typing import Any
 
 import ray
@@ -214,7 +215,10 @@ class _GatewayActor:
         """Start the FastAPI server backing this gateway actor."""
         if self._server_task is not None:
             return
-        self._server_port, self._server_task = await run_uvicorn(self._app, None, self._server_address)
+        _gateway_port = int(os.environ.get("GATEWAY_PORT", "0"))
+        self._server_port, self._server_task = await run_uvicorn(
+            self._app, None, self._server_address, port=_gateway_port
+        )
         host = f"[{self._server_address}]" if is_valid_ipv6_address(self._server_address) else self._server_address
         self._server_base_url = f"http://{host}:{self._server_port}"
 
