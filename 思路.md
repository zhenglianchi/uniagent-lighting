# 分布式代码智能体强化学习平台 技术实现文档
## 文档版本：V4.4（2026-08-11）

## 更新说明

V4.4（2026-08-11）：

1. **黑盒采样重启（反转 V4.3 的"不改"决策）**：2026-08-11 v0.35.1 实现腾讯 E2B
   direct-URL 版 `claude_code_runner`（Claude Code 直连公网 Gateway + 沙箱内 npm 装
   （详见 §1.10 与 TODO §G）。
2. **优化路线定稿**：PD 分离彻底放弃（HumanEvalFix 短 prompt 收益有限 + 无 RDMA
   Mooncake 收益有限）；**双机全异步（verl v1 `colocate_async`/`separate_async`）为
   网络就绪后第一优先**（§6.4 调研：配置级开关，无需改 rollouter；理论 1.6-1.8x）。
3. **训练与评测里程碑**：HumanEvalFix 全样本 baseline 26 步（基座 76.4% → final
   83.2%）；投机解码（EAGLE-3 + LoRA merge）25 步（吞吐 +41.7%，final 82.61%）；
   gateway 解析容错方案 A 上机验证通过。
4. **腾讯沙箱配额已提升**（用户控制台操作），但后续并发保持 baseline 口径
   （64 / max_num_seqs 128 / util 0.8），保证速度对比可比。

V4.2（2026-08-06）：

1. **训练基座切换 Qwen2.5-Coder-7B → Qwen3-8B**（Qwen2.5-Coder 官方不支持标准
   function calling，agent 只写散文不调工具 → reward 恒 0）；Qwen3-8B 工具调用验证通过。
2. **修复 4 个 agentic 链路 bug**（uni-agent + runner + reward 三处）：
   - 并发 session 共用固定 `/tmp` 配置 → agent attach 错沙箱（v0.22.0）
   - `pytest -q` 点号输出解析失败 → score 恒 0 假象（真实 20/21 = 0.9523，v0.22.1）
   - reward_info 键名不匹配（`reward_score` vs `reward`）→ reward 进不了 verl（v0.22.2）
   - 参数化测试名被 shell 展开破坏（v0.23.1）
   （8+ 条样本、温度 0.7/1.0、上下文 16k/32k）全部 60 轮不修改代码，出现死循环
   （echo/cat/git diff 复读）。极简单函数修复任务（simple-bench）实验：部分修复
   成功（simple_reverse 0.5）但训练仍无法形成组内梯度，**已回滚**。
4. **当前基线**：agentic GRPO 链路全通（工具调用、腾讯沙箱、真实 reward、checkpoint
   保存），reward 0.9523 真实进入训练 metrics；缺的是**正样本/组内差异**。
   下一步（当时）：保存 node2 镜像 → 换更强基座或 SFT 预热（已由 V4.3 更新）。

V4.3（2026-08-06）：

1. **下一步定稿：换数据集 = HumanEvalFix**（`bigcode/humanevalpack` Python 修复子集），
   Qwen3-8B 在长程任务上行为退化已实证（60 轮不调工具），HumanEvalFix 单函数修复
   8B 可解（60 轮内出结果），
   且 8B 级模型有公开 pass@1 基准可对照，绕开长程探索瓶颈。
2. **黑盒 agent（Claude Code 类）调研结论（决定不改，留档）**：技术上可把 Claude Code
   当编排器、模型换成可训练的 Qwen3-8B（vLLM 原生 Anthropic `/v1/messages` +
   `ANTHROPIC_BASE_URL`），但① Anthropic 官方 "unsupported"（issue #5577）+ ToS 灰色
   （禁止第三方 harness 用订阅、训练竞对模型）；② Claude Code 系统提示面向 Claude，
   8B 未必接得住；③ 需 Anthropic ↔ OpenAI/verl 格式桥 + 隐藏 system blocks 清洗，
   数天工作量。详见 1.10。
3. **优化路线确认（跑通后按序）**：双机全异步 → 投机解码（已由 V4.4 更新：PD 分离放弃，
  投机解码已完成）。
4. **工作流**：服务器已关机、node2 镜像已保存；所有代码/文档进 uniagent-lighting 仓
   （每任务 commit + CHANGELOG + 版本递增），恢复后 `git pull` 即可。

V4.1：新增 1.8「agent-lightning runner 调研」——克隆 microsoft/agent-lightning 到本地（work/agent-lightning）研究其 Runner/Store/Algorithm 解耦设计与多 agent 实例并发模型，确认与 uni-agent 的联动边界与可借鉴点（详见 1.8）。

V4.0 相对 V3.0（腾讯云 HAI 版）的核心变化（背景详见 `TODO.md` 文末「为什么舍弃 HAI」）：

1. **训练平台：腾讯云 HAI → UCloud/智星云**：verl 0.9 只支持 vllm V1（硬性要求 GPU CC≥8.0），HAI 的 T4（7.5）/V100（7.0）不满足，实测无法跑 GRPO，弃用。新平台卡型 A800/A100/4090（CC≥8.0），普通 GPU 云主机支持多机内网组网。
2. **存储：COS → UCloud 机器本地 / SFTP 直传**：模型、数据集、checkpoint 直接放 UCloud 本地磁盘或本地 SFTP 直传，不再走对象存储。
4. **采样端不变**：本地 WSL2 跑 mini-swe-agent；测试期调阿里云百炼 API（`qwen3.7-plus`，100 万 token 额度），正式期调云端 vLLM server / Uni-Agent Gateway。

---

# 第一部分：总体方案

## 1.1 项目目标

搭建**采样-训练分离**的分布式代码智能体强化学习平台：

- 云端 UCloud（CC≥8.0 GPU）运行 Uni-Agent/VeRL，负责 vLLM rollout 与 GRPO 训练；
- 本地采样器与云端训练通过 SFTP 直传 / UniAgent API 异步解耦；
- 通过"单机双卡调试 → 单机多卡实验 → 多机 VPC 分布式"渐进式落地（多机是校招项目需求：学习多机通信）。

## 1.2 技术栈

| 模块 | 选型 | 说明 |
| --- | --- | --- |
| 本地开发环境 | Windows + WSL2 Ubuntu24.04、Miniconda（swe-rl env） | 采样端 |
| 轨迹上传 | trajectory uploader | JSONL 缓冲 + zstd（实测压缩率 ~9.4%）+ SFTP 直传 UCloud；Uni-Agent API 直传留作阶段二对照 |
| 训练框架 | Uni-Agent + VeRL | GRPO，云端编排；Uni-Agent 为 verl 官方社区开源框架（verl-project/uni-agent），构建于 verl 之上 |
| 模型推理 | 采样推理：阿里云百炼 API（测试期）/ 云端 vLLM server 或 Uni-Agent Gateway（正式期）；训练 rollout：VeRL 内置 vLLM | 训练侧不单独启动 OpenAI-compatible vLLM server |
| 训练方式 | 基座 Qwen3-8B（BF16）+ LoRA（rank=32）/ FSDP2 | 单卡 4090 冒烟用 FSDP2 + CPU 卸载错峰（见 TODO §C） |
| 分布式调度 | Ray | 仅部署在云端/VPC 内网，不做公网 Ray 集群 |
| 算力平台 | UCloud（首选）/ 智星云（备选） | CC≥8.0；多机同 VPC 内网互通 |
| 远端存储 | UCloud 机器本地磁盘 | 模型、数据集、checkpoint；不再走 COS |
| 序列化 | JSONL + zstd（基线） | 阶段二用 Arrow + zstd / Parquet + zstd 做 A/B |
| 权重同步 | VeRL vLLM rollout 权重更新 | 阶段三可引入 Mooncake P2P Store / checkpoint-engine |

## 1.3 系统架构图

```
【本地端 WSL2 Ubuntu】
mini-swe-agent
└ 轨迹采样（JSON）——模型端点：测试期=阿里云百炼 API（qwen3.7-plus）；正式期=云端 vLLM / Uni-Agent Gateway
trajectory uploader
├ zstd 压缩（基线）
├ 批量上传
└ SFTP 直传 UCloud（/home/<user>/swe-rl/trajectories/）
        ↓ SFTP
【云端 UCloud GPU（CC≥8.0）】
Uni-Agent（agent 编排 + Gateway：采样请求进→训练 token 出）
└ VeRL
   ├ vLLM rollout（训练用，VeRL 内置，GPU 统一调度；TransferQueue 阶段二 A/B）
   ├ GRPO trainer（FSDP2 / LoRA）
   └ Ray（云内网调度）
        ↓ 训练闭环：每轮权重更新 → Gateway/vLLM 中模型随之更新
【UCloud 本地磁盘】
```

## 1.4 数据链路与关键边界

1. **本地不部署 TransferQueue**：本地只有 trajectory uploader；VeRL TQ 在云端 rollout 内部。
2. **两条推理链路要区分**：
   - **采样推理**（本地 mini-swe-agent 调用）：测试期指向阿里云百炼 API；正式期指向云端单独启动的 OpenAI 兼容 vLLM server 或 Uni-Agent Gateway（二者任一）。此链路是"请求进、生成轨迹"。
   - **训练 rollout**（GRPO 生成）：由 VeRL 内置 vLLM rollout 完成，**不单独启动 8000 端口 vLLM server**，权重由 trainer 每轮更新。此链路是"模型出、算 reward"。
   - 二者不要混淆：思路.md 中"不单独启动 OpenAI-compatible vLLM server"仅指训练 rollout；采样侧正式期必须有一个可访问的推理端点。
3. **轨迹上传**：WSL → UCloud（SFTP 直传，JSONL+zstd+manifest）→ 云端消费，作为阶段一基线；Uni-Agent API 直传作为阶段二对照。
4. **数据来源（2026-08-06 换数据集定稿）**：训练/采样数据 = **HumanEvalFix**
   （`bigcode/humanevalpack` Python 修复子集：单函数 buggy 代码 + 单测，8B 60 轮内可出结果）；
   （本地 `work/models/` + UCloud `/home/ubuntu/models/` 各一份）。
5. **权重同步闭环**：VeRL trainer 每轮更新后调用 vLLM rollout 权重更新，不需要手工重启 vLLM；正式采样若走 Uni-Agent Gateway，Gateway 内模型随训练轮次同步，实现"采样用的就是正在训练的模型"。

## 1.5 分阶段演进路线

### 阶段一：UCloud 单机（当前执行阶段）

硬件：单台 CC≥8.0 GPU（4090 24G / A800 / A100），内存 ≥64GB（32GB 实测不够）。

目标：打通"采样 → 轨迹上传 → GRPO 训练 → 权重同步到 rollout"的完整闭环。

约束：

- verl 0.9 + vllm V1（CC≥8.0 前提）；单卡冒烟用 FSDP2 + CPU 卸载错峰；
- 采样端 = 本地 mini-swe-agent + 腾讯云沙箱。

交付目标：一轮完整「采样 → 轨迹传输 → GRPO 训练 → vLLM 权重更新」稳定运行
（2026-08-06 起数据口径换 HumanEvalFix，agent 不变）。

### 阶段二：UCloud 单机多卡（正式对照实验阶段）

升级为单机双卡/多卡实例，逐项做 A/B 对照：

1. VeRL 内置 `transfer_queue` 开/关；
2. JSONL+zstd vs Arrow+zstd vs Parquet+zstd；
3. VeRL HybridEngine 显存复用策略；
4. WSL SFTP 直传 vs UniAgent API；
5. **双机全异步 GRPO（2026-08-11 用户定稿：第一优先）**：verl v1
   `trainer_mode=colocate_async`（单机）/ `separate_async`（双机）+ TQ SimpleStorage，
   Trainer 与 rollout 重叠（实测 gen 53% + update 33%，理论 1.6-1.8x）；Mooncake 不单跑。
6. **rollout 性能优化**（agentic RL 瓶颈在生成）：
   - **LoRA + vLLM 引擎常驻（adapter 热插）**：训练侧用 LoRA 保持轻量（训练方式非项目重点），每步只热插 ~100MB adapter、vLLM 引擎不重建——先于 PD/投机解码落地，收益最直接；
   - **投机解码（Speculative Decoding，✅ 2026-08-10 完成）**：EAGLE-3（Qwen3-8B
     官方 speculator）+ `lora.merge=True`，实测吞吐 +41.7%；修 logprobs 0 全丢与
     merge 权重物化 bug（verl#7014）。
   - **PD 分离（Prefill/Decode Disaggregation）**：~~2026-08-11 用户放弃~~——
     短 prompt 任务收益有限，不做；全异步（上文第 5 条）取代其地位。
   - 详见 `TODO.md` §C 6.5。

产出指标：单轮迭代耗时、样本吞吐、GPU 利用率、网络传输量、显存峰值。

### 阶段三：UCloud 多机分布式（最终架构形态）

- 多台实例同一 VPC/子网，Ray 云内网组网，NCCL 通信；
- Mooncake P2P Store / checkpoint-engine 做云端权重分发（普通款无 RDMA 时收益待实测）；
- 验证多机扩展性与权重同步耗时。

## 1.6 核心创新点（简历口径）

1. **采样/训练物理分离**：本地 WSL 采样 + 云沙箱执行，云端 GPU 训练，算力按需租用。
2. **轨迹数据管道**：本地缓冲 + JSONL/zstd 序列化 + SFTP 直传，并对 JSON/Arrow/Parquet 做压缩与吞吐对比。
3. **基于 VeRL 的 rollout/训练优化**：使用 VeRL 内置 TQ 与 HybridEngine 显存复用做 A/B 实验，不宣称"自研 TransferQueue"。
4. **云端多机优化**：Ray + NCCL 多机训练 + verl v1 全异步（colocate/separate）；
   双机权重同步走 checkpoint-engine（nccl/nixl/mooncake 可选，separate_async 必需）。
5. **rollout 生成优化**：以 **LoRA + vLLM 引擎常驻（adapter 热插）**为底座，对
   **投机解码（已实测 +41.7%）、全异步（v1 colocate/separate_async）**等针对
   agentic RL 生成瓶颈的优化做系统化 A/B（训练方式本身非项目重点，详见 TODO §C 6.5）。

简历中不要写"自研 TransferQueue/显存分时调度"；统一写成"基于 VeRL/UniAgent 的配置级优化与系统化 A/B 验证"。

## 1.7 显存资源分配

- 当前实际（2026-08-08 起）：单机 48G（node1）+ LoRA rank=32 + vLLM util 0.5~0.8，
  训练显存峰值 ~19G、内存峰值 ~65G（94G 无压力）；历史 24G 错峰方案（FSDP2 +
  CPU 卸载 + vllm sleep）仅留作参考（TODO §C 6.1），双卡 24G 已放弃（FSDP 参数与
  vLLM 引擎无法共存）。
- 多机：由 VeRL 统一管理，不手工固定 `GPU0=vLLM / GPU1=trainer`；LoRA 方式下显存
  压力明显降低。

## 1.8 agent-lightning runner 调研（2026-08-04，本地 work/agent-lightning）

背景：实习项目用 agent-lightning，本项目须换 uni-agent；调研其 runner 设计，确认是否能与 uni-agent 联动、如何跑多个 agent 实例，作为我们训推平台的设计参考。

### 1.8.1 agent-lightning 的核心架构（v0.2+）

**三方解耦**：Algorithm（算法/训练）↔ **LightningStore**（唯一共享点）↔ Runner（长驻 worker）。Runner 与 Algorithm 之间**零直接通信**，官方明确"runner 与 algorithm 可独立伸缩"：

- **Runner = 长驻 worker 进程**：`LitAgentRunner` 通过 `store.dequeue_rollout(worker_id)` 轮询任务队列 → 执行 `LitAgent`（用户自定义 agent 逻辑）→ 把 Rollout 写回 store；带 heartbeat（默认 10s）和 poll_interval（默认 5s + jitter）。
- **Runner 与 agent 解耦**：LitAgent 是薄封装，runner 只负责"取任务→执行→记录"，任何 agent（OpenAI 兼容 harness、Codex/Claude-Code 等）都能包成 LitAgent 注入；模型调用经 **LLMProxy**（HTTP 端点作为 store 资源下发）。
- **多 agent 实例 = 复制 RunnerBundle**：`ExecutionStrategy` 决定复制方式——`SharedMemoryExecutionStrategy`（同进程多线程，n_runners）+ `ClientServerExecutionStrategy`（跨进程 HTTP：`LightningStoreServer` + `LightningStoreClient`，可跨机器）。多个 runner worker 并发消费同一队列即"跑多个 agent 实例"。
- 可观测：hooks（on_rollout_start/end）+ OpenTelemetry tracer，轨迹/span 全量入库。

### 1.8.2 与 verl / uni-agent 的关系（联动边界）

- **agent-lightning 的 verl 集成是 verl wrapper，不是 uni-agent wrapper**：`agentlightning.verl`（`AgentLightningTrainer` + `AgentModeDaemon`）直接 `import verl.trainer.main_ppo` 并替换其 sampler/daemon。它依赖的是 verl 本身，没有针对 uni-agent 的接口 → **不能把 uni-agent 直接塞进 agentlightning.verl**（会变成"第二套编排层"），只能二选一。
- **uni-agent 原生等价物（已内置，无需 agent-lightning）**：`AgentRunner` Protocol（async callable：`session / raw_prompt / sample_index`）+ 配置 `agent_runners.<name>.runner_fqn`（用户自定义 runner 全限定名）+ `dispatch_mode`（`inline_async` / `ray_task`）+ `max_concurrent_sessions`（并发上限）+ `trajectory_selection`。示例 `examples/inference/parallel_infer_verl.py`：`rollout.n` 个 gateway session 并发跑，`GLOBAL_CONCURRENCY` 控制 in-flight 数。uni_agent.agents 已有 mini_swe_agent / react / claude_code 三个内置 agent 示例。

**结论**：agent-lightning 的 runner 不能（也不需要）与 uni-agent 直接联动——uni-agent 已原生提供同样的能力（用户自定义 agent runner + 多实例并发限流）。agent-lightning 的价值是**设计参考**：Store 解耦、runner 横向扩展、心跳与可观测。

### 1.8.3 对我们训推平台的启发

1. **解耦点 = 轨迹缓冲**：agent-lightning 的 Store 在我们平台对应"trajectory uploader 本地缓冲 + 云端轨迹目录/队列"——采样端（runner）与训练端（trainer）只通过轨迹交换，天然支持"用户在本地填云端 uni-agent 端口 → 轨迹异步上传 → 云上 GRPO 训练"的训推平台形态。
2. **用户自定义 agent 的插件点 = runner_fqn**：平台暴露"注册一个 async AgentRunner callable"即可接入任意 agent（我们已有的 tencent_e2b 沙箱适配器 + mini-swe-agent harness 可封装成 uni-agent agent/runner，作为默认内置实现）。
3. **多实例并发与成本**：`max_concurrent_sessions` 即并发上限；配合腾讯云沙箱按秒计费，可按"并发数 × 单条耗时"精确估算采样成本（4C8G ≈ 1.9 元/小时/实例）。
4. **可借鉴但非必须**：heartbeat（沙箱实例存活/任务进度上报）、hooks（rollout 开始/结束上报轨迹）、OpenTelemetry 可观测——后续做云端采样监控时按需引入。

### 1.9 Agent 部署形态（2026-08-06 定稿，防跑偏）

**agent harness 在沙箱外，沙箱只是执行环境**：

- **mini-swe-agent（harness）运行在训练机**（runner 进程内），通过环境类（`tencent_e2b` attach 模式）向腾讯云沙箱发命令/读写文件——与本地采样链路同构（本地 WSL harness + 云沙箱环境），训练时只是把 harness 移到训练机由 runner 驱动；
- **不把 agent 装进沙箱**（黑盒模式是 claude-code 这类自包含 CLI 的形态，mini-swe-agent 不是）；
- 模型端点 = 训练机 Gateway（沙箱内 SSH 隧道走 22 转发，`docs/vllm_access.md`）；
- 轨迹由 Gateway session 记录；reward 在沙箱内 test_patch 评估（不注入 agent）；
- 每个样本一个沙箱实例、用完即毁（`StartSandboxInstance`/`StopSandboxInstance`）。

> **2026-08-06 v0.14.0 修正**：harness 在训练机时**直接调本机 Gateway**
> （`session.base_url`，无需隧道）——SSH 隧道只对"agent 装进沙箱"的黑盒模式有意义，
> agent-outside 模式下沙箱内 E2B 连接走的是节点到腾讯的出方向，不需要公网入方向。

### 1.10 黑盒 agent（Claude Code 类）调研与决策（2026-08-06）

**结论（2026-08-06 初判：不改 agent，继续白盒；2026-08-11 反转：黑盒方案启动）**。
2026-08-11 v0.35.1 已实现腾讯 E2B direct-URL 版 `claude_code_runner`（见
uniagent-lighting 仓库与 TODO §G），纯函数测试通过、待服务器开机上机验证。下方
调研内容留档，其中阻断点 1/3 已通过"模型换 Qwen3-8B（可训练）+ pin claude-code
2.1.153 + vLLM 原生 `/v1/messages` + Gateway anthropic adapter 现成"解决：

- **可行技术路线（留档）**：Claude Code CLI 支持 `ANTHROPIC_BASE_URL` /
  `ANTHROPIC_AUTH_TOKEN` 指向任意 Anthropic 兼容端点；vLLM 原生提供 `/v1/messages`
  （含 `/v1/messages/count_tokens`）。可把 Claude Code 当"编排器"（黑盒），模型换成
  我们可训练的 Qwen3-8B（权重随 verl GRPO 更新），Claude Code 装进腾讯沙箱实例跑，
  轨迹在 Gateway/vLLM 侧全量捕获后清洗成 verl messages 格式 → 仍可做 GRPO。
- **阻断点**：
  1. Anthropic 官方明确 "using Claude Code with non-Anthropic models is unsupported"
     （github issue #5577）；2026-02 条款更新禁止第三方 harness 使用 Claude 订阅（OAuth）。
  2. Claude Code 系统提示（~几十 KB + prefill / 并行工具特性）面向 Claude 设计，
     8B 小模型未必接得住，需先实测。
  3. Anthropic Messages ↔ OpenAI/verl 格式桥 + 隐藏 system blocks 清洗 + n 条 rollout
     轨迹还原，是数天工程量。
  4. 若用完整 Claude 模型（编排器 + 模型都是 Anthropic），则无法 GRPO（权重不可训练），
     只能 SFT 蒸馏/评测，且触碰"训练竞对模型"条款风险。
- **开放替代（如 Claude Code 路径受阻）**：opencode / crush（MIT，Claude Code 风格），
  原生接 OpenAI 兼容端点，无格式/授权障碍；暂不引入。

---

# 第二部分：分步落地实现

## 2.1 前置准备清单

1. UCloud 账号（或智星云备选），GPU 云主机 CC≥8.0，系统盘 ≥100GB；
2. 腾讯云 Agent Runtime（沙箱）开通，API Key / CAM 密钥（`work/tencent_sandbox.env`）；
3. Windows 安装 WSL2 Ubuntu24.04 + Miniconda（swe-rl env）；
4. 记录 WSL 公网出口 IP，用于安全组白名单；
5. 模型 `Qwen3-8B`（本地 + UCloud 各一份；Qwen2.5-Coder-7B 因不支持标准 function

## 2.2 网络安全配置

1. UCloud 安全组：放行 SSH 22、Ray 6379/6381/6382/8265/10001、NCCL 动态端口（内网段全放最省事）。
2. 两台实例**同地域同可用区 + 同 VPC/子网**（内网互通前提）；SSH 免密 + `/etc/hosts` 互指。
3. Ray dashboard 无内置鉴权，优先通过 SSH 隧道访问：`ssh -L 8265:127.0.0.1:8265 user@<IP>`。
4. WSL 公网出口 IP 动态变化，正式配置安全组前重新确认。

## 2.3 云端环境初始化

```bash
# miniforge（Python 3.10）→ 清华 pip 源 + HF 镜像永久配置
git clone --recursive https://github.com/verl-project/uni-agent.git
pip install --no-deps -e ./verl
pip install -e .                                   # uni-agent 本体
pip install TransferQueue==0.1.9                   # verl v1 transfer_queue 后端必需
# 版本链（2026-08-04 起实测）：torch 2.9.0+cu128 / vllm 0.11.1（多机硬性要求）/
#   transformers 4.57.x / verl 0.9.0.dev（uni-agent 捆绑）/ ray 2.56.1 / TransferQueue 0.1.9
```

⚠️ verl 补丁（已内嵌 `setup_ucloud_uniagent.sh`）：① py3.10 StrEnum 兼容；② 单卡 fsdp2 跳过冗余 `module.state_dict()` 拷贝+回灌。

## 2.4 数据准备

1. 模型：`hf-mirror` 下载 **Qwen3-8B**（16GB，`HF_HUB_DISABLE_XET=1`）——训练基座
   （Qwen2.5-Coder-7B 不支持标准 function calling，已弃用，V4.2）。
2. 冒烟/训练数据：**HumanEvalFix**（`bigcode/humanevalpack` Python 子集）→ 每样本
   `solution.py` + `test_solution.py` + FAIL_TO_PASS → agentic 数据字段
   `data_source` / 消息列表 `prompt` / `tools_kwargs.task`（文件注入）/
   `reward_model.ground_truth` / `ability`（verl agentic RL 必需）。

## 2.5 VeRL 配置要点

完整冒烟配置见 `TODO.md` §C 6.1（`run_grpo_smoke_ucloud.sh`）。要点：

**GRPO 配置口径（已确认）：**

- `n=4`（GRPO group size：每个 prompt 生成 4 条回答）与 `batch_size` 是**两个维度**：一个训练 step 的生成序列数 = `batch_size × n`。
- 冒烟阶段推荐：`batch_size=8`（每 step 8 个 prompt → 32 条生成）、`micro_batch_size=2`、`total_training_steps=10~20`、rollout `max_num_seqs=16`；跑通后再升。
- **全样本定稿（2026-08-08 起实测）**：HumanEvalFix train161 / `batch=32` / `mini=16` /
  `micro=4` / `n=4` / 并发 64 / vllm `max_num_seqs=128` / `util=0.8` / 5 epoch；
  投机 run 另加 `lora.merge=True` + EAGLE-3。并发口径与 baseline 一致，配额提升
  不作为加速手段（2026-08-11 用户定）。
- 原则：**先小后大**——任何数值先跑通，再看显存余量往上加，OOM 就降。
- 成本澄清：100 万 token 是阿里云 Qwen API 额度，**只花在本地 mini-swe-agent 采样**；云端 GRPO rollout 用本地部署模型（vLLM），不消耗 API 额度，只计 GPU 时长。

## 2.6 启动 UniAgent/VeRL

```bash
cd /root/swe-rl
verl train --config-path config --config-name verl_config
```

1. 使用 UniAgent 官方启动命令，不要同时手动启动独立 vLLM server。
2. 使用 LoRA 时确认 vLLM rollout 侧的 LoRA adapter 更新配置已开启。
3. 观察首轮日志：rollout 正常生成；GRPO loss 有数值；训练结束后 vLLM 权重同步事件无报错。

## 2.7 本地 WSL 采样端部署

1. mini-swe-agent（swe-rl env，可编辑安装）+ 腾讯云沙箱 `tencent_e2b` 环境类（已注册）。
4. 轨迹上传：`trajectory_uploader.py`（JSONL+zstd → UCloud SFTP 直传）。
5. 若使用 UniAgent API：本地不连接 Ray，只调用 UniAgent 上传接口，上传后由云端入库并进入 VeRL。

## 2.8 调试验证顺序

### 测试 1：UCloud 本地闭环自测

- 不启动 WSL 采样；用冒烟数据跑 UniAgent/VeRL；
- 验证模型加载、GRPO、vLLM 权重更新均正常（单卡冒烟已推进到训练步，32GB 内存不够，换 ≥64GB 后预期直接通过）。

### 测试 2：跨端轨迹上传

- WSL 上传测试轨迹；云端确认 JSONL 解析、zstd 解压、schema 校验通过。

### 测试 3：完整端到端闭环

- WSL 持续采样（腾讯云沙箱）→ 云端完成一轮训练 → 验证下一轮 rollout 使用更新后的策略权重；
- 记录指标：单轮迭代耗时、样本吞吐、GPU 利用率、网络传输量。

### 测试 4：恢复与容错

- 关机重启后从本地 checkpoint 恢复；公网 IP 变化后更新连接配置。

## 2.9 成本管控规范

1. 调试结束立即关机，减少按量计费时长；
2. 权重、数据集、checkpoint 放 UCloud 本地盘，不额外买对象存储；
3. 启动脚本提前写好，开机直接运行；
4. 环境稳定后保存自定义镜像 / 重建脚本（`setup_ucloud_uniagent.sh`）。

## 2.10 阶段二/三升级路径

1. 单卡链路跑通 → 保存镜像；
2. 双卡/多卡实例复用同一 setup 脚本；
3. 阶段二逐项 A/B（transfer_queue / 序列化 / HybridEngine / 上传通道）；
4. 阶段三同 VPC 多机组 Ray + NCCL；全异步 separate_async 需非 naive checkpoint
   engine（nccl/nixl/mooncake）做权重同步，上机后按实测选型。

---

# 第三部分：简历项目简述


项目采用渐进式落地：单机打通全链路（HumanEvalFix 数据，Qwen3-8B + LoRA GRPO，
全样本 26 步训练，基座 76.4% → final 83.2%），rollout 侧做 EAGLE-3 投机解码
（吞吐 +41.7%）与 gateway 工具解析容错等系统化 A/B，多机阶段在同 VPC 内扩展
Ray 集群并验证 verl v1 全异步（colocate_async / separate_async，Trainer 与 rollout
重叠）。最终以单轮迭代耗时、样本吞吐、GPU 利用率、显存峰值和网络传输量等指标
量化优化效果。

---

# 风险清单与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| 单卡 OOM | 7B GRPO 超出 24GB | FSDP2 + CPU 卸载错峰；LoRA 备选 |
| 内存不足 | fsdp2 + vllm + ray 超 32GB 死机 | 换 ≥64GB 内存机型（已拍板） |
| 多机带宽低 | 普通款无 RDMA，10~25G TCP | 接受并记录指标；全异步权重同步阶段三评估 |
| 公网 Ray 不稳定 | 端口/IP/对象传输问题 | 优先 SFTP/API；Ray 只留云内网 |
| 版本不兼容 | verl/vllm/torch 冲突 | 按实测版本链锁定（见 TODO §C） |
| 腾讯云沙箱计费 | 商业化计费 | 按需起停，跑完即销毁 |
