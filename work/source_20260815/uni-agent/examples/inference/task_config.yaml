- name: swe_bench
  sandbox:
    provider: modal
  agent:
    name: react
    max_steps: 200
    tools:
      - name: stateful_shell
        command_timeout: 180
        env_vars:
          PIP_PROGRESS_BAR: "off"
          PIP_CACHE_DIR: "~/.cache/pip"
          PAGER: "cat"
          MANPAGER: "cat"
          LESS: "-R"
          TQDM_DISABLE: "1"
          GIT_PAGER: "cat"
      - name: str_replace_editor
      - name: submit
    model:
      temperature: 0.8
      top_p: 0.9
      max_total_tokens: 65536
