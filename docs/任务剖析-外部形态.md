# 任务级剖析：外部形态同一条 reward=1 轨迹（超详细版）

> 用与「内部形态剖析」**同一条真实任务**（humanevalfix-Python-111，reward=1.0）
> 走外部协议逐步推演。外部形态 P0 单步闭环已于 2026-08-12 验证（reward=1.0），
> 但该次轨迹未归档；本文任务内容、轨迹结构与内部形态一致（都由 Gateway 云侧
> 物化），差异只在**任务交接方式与网络路径**。每步标注：发生在哪台机器、哪个
> 文件哪一行、文件在哪个位置、该任务当时的状态。

## 1. 机器与进程拓扑（外部形态）

```text
┌─ 本地 WSL（用户机器）─────────────────────────────────────────────┐
│ platform_local_agent.py 进程                                      │
│  ├─ paramiko SSH/SFTP（连训练机，读 task.json / touch done）       │
│  ├─ TunnelForwarder：127.0.0.1:<随机端口> → 训练机内网 Gateway     │
│  └─ mini-swe-agent（agent 本体，模型调用走隧道）                    │
└────────────────────────────────────────────────────────────────────┘
        │ SSH(公网) / 隧道                                           │
        ▼                                                            │
┌─ 训练机 node1 ────────────────────────────────────────────────────┐│
│ Ray：trainer + vLLM + GatewayActor(:8001) + AgentFrameworkWorker   ││
│ external_agent_runner（建沙箱/写 task.json/等 done/reward）         ││
│ 文件：/home/ubuntu/swe-rl/platform_test/<session>.task.json        ││
└────────────────────────────────────────────────────────────────────┘│
        │ E2B                                                        │
        ▼                                                            │
┌─ 腾讯云沙箱（工具执行）────────────────────────────────────────────┘
│ /testbed/solution.py（训练机 runner 注入；agent 通过 attach 使用）   │
└─────────────────────────────────────────────────────────────────────
```

**外部形态**：agent 在用户本地 WSL，训练机只提供 Gateway（模型+轨迹）、沙箱
（执行）、reward；两端通过 `task.json`（任务下发）和 `done` 标记（完成回报）
交互。

## 2. 任务本体（同内部形态）

humanevalfix-Python-111：修复 `histogram(test)` 空串 bug；hidden test
`test_solution.py::test_all` 8 条断言；buggy 代码与修复方向见
`docs/任务剖析-内部形态.md` §2。

## 3. 逐步运行剖析

### 3.0 训练侧启动（先起）

- **位置**：训练机 node1，shell 后台进程。
- **命令**：`MODEL=/home/ubuntu/models/Qwen3-8B-final setsid nohup bash
  scripts/train/run_grpo_platform_test_ucloud.sh > grpo_platform_test_baseline.log
  2>&1 < /dev/null &`。
- **代码链**：`main_ppo`（`verl/trainer/main_ppo.py:129`）→
  `init_agent_loop_manager`（107，按配置加载
  `AgentFrameworkRolloutAdapter`）→ `fit` → 采样时调 `external_agent_runner`
  （`uni_agent_ext/agents/external_agent_runner.py:86`）。
- **该任务状态**：训练侧等待 sample 20 采样请求；`/home/ubuntu/swe-rl/platform_test/`
  目录为空；本地 WSL 还没动作。

### 3.1 external_agent_runner：提取任务 + 建沙箱 + 注入 solution.py

- **位置**：训练机 node1，Ray 集群内 runner worker；**沙箱在腾讯云创建**。
- **代码**：`external_agent_runner.py`
  - 96-98：`extract_task_meta(raw_prompt, tools_kwargs)`（来自
    `mini_swe_agent_runner.py:135`）→ instance_id / fail_to_pass / buggy solution.py；
  - 101：`create_task_sandbox(image=python:3.12, gateway_url=session.base_url)`
    （`mini_swe_agent_runner.py:177`）→ `sandbox.start()`（腾讯 E2B 实例）；
  - 106-124：沙箱内 `git init` + 写 `/testbed/solution.py`（buggy）+
    `git add -A`。
- **该任务状态**：沙箱实例创建；**沙箱内文件 = buggy solution.py**；隐藏测试未注入。

### 3.2 写 task.json（任务下发到文件）

- **位置**：训练机 node1 磁盘（`/home/ubuntu/swe-rl/platform_test/`）。
- **代码**：`_write_task_file`（`external_agent_runner.py:47`）→
  `PLATFORM_TEST_DIR / f"{session.session_id}.task.json"`，payload（本任务实例）：

```json
{
  "session_id": "session-sample-20-rollout-1-xxxx",
  "base_url": "http://10.60.216.3:8001/sessions/session-sample-20-rollout-1-xxxx/v1",
  "instance_id": "<腾讯沙箱实例>",
  "image": "python:3.12",
  "raw_prompt": "<题目全文>",
  "tools_kwargs": {"env": {"files": {"solution.py": "<buggy 版>"}, ...}},
  "task": {"instance_id": "humanevalfix-Python-111", "issue": "<题目>"},
  "done_marker": "/home/ubuntu/swe-rl/platform_test/session-sample-20-rollout-1-xxxx.done",
  "created_at": 1755...
}
```

- **该任务状态**：**服务器上出现 task.json**；训练侧进入 `_wait_for_done`
  （`external_agent_runner.py:77`，每 5s 轮询 done 文件）。

### 3.3 本地认领任务（后起，原子认领）

- **位置**：本地 WSL → 训练机（SFTP）。
- **命令**：`PYTHONPATH=$PWD/vendor/uni-agent:$PWD python
  scripts/platform/platform_local_agent.py --wait --timeout 1800`。
- **代码**：`main`（`platform_local_agent.py:277`）→ paramiko 连接（公网 IP，
  `work/ucloud.env`）→ `wait_for_task`（127）：
  - 每 5s `sftp.listdir(remote_dir)` 找 `*.task.json`；
  - 先 `_recover_stale_claims`（161）回收过期 `.claimed`；
  - `sftp.rename(<name>, <name>.claimed)` **原子认领**（谁 rename 成功谁拥有）。
- **该任务状态**：本地读到本任务 payload，打印 `task: session-sample-20-... 
  instance=humanevalfix-Python-111`；**服务器文件从 `.task.json` 变成
  `.task.json.claimed`**（认领成功）。

### 3.4 SSH 隧道

- **位置**：本地 WSL（TunnelForwarder 线程）。
- **代码**：`TunnelForwarder`（`platform_local_agent.py:67`），
  `direct-tcpip` 通道：本地 `127.0.0.1:<随机端口>` → 训练机内网
  `10.60.216.3:8001`（`parsed.hostname` 来自 task.json 的 base_url）。
- **该任务状态**：`tunnel up: 127.0.0.1:43210 -> 10.60.216.3:8001`；后续 agent
  模型请求全部走这条 SSH 加密通道。

### 3.5 生成 mini-swe config

- **位置**：本地 WSL，config 写本地 `/tmp`。
- **代码**：`build_config`（186）→ `build_mini_swe_config`
  （`mini_swe_agent_runner.py:264`）：
  `base_url=http://127.0.0.1:43210/sessions/<id>/v1`（隧道后 Gateway 端点）、
  `model=Qwen3-8B`、`max_turns=60`、`instance_id=<腾讯沙箱>`、`image=python:3.12`。
- **该任务状态**：本地 `/tmp/platform_mini_swe_<session尾8位>.yaml` 就绪；agent
  将 attach 云端已建沙箱。

### 3.6 本地跑 agent（6 轮）

- **位置**：agent 逻辑在**本地 WSL**（`run_local_agent`，
  `platform_local_agent.py:207`）；**模型调用经隧道到训练机 Gateway+vLLM**；
  **工具执行在腾讯沙箱**。
- **代码**：`run_local_agent` → `get_sb_environment` + `get_agent` →
  `agent.run(problem_statement)`，`ThreadPoolExecutor` + `future.result(timeout)`
  兜底。
- **每轮模型调用路径**（与内部形态唯一差别 = 网络路径）：

```text
mini-swe-agent（本地 WSL）
  → POST http://127.0.0.1:43210/sessions/<id>/v1/chat/completions
  → TunnelForwarder（本地，platform_local_agent.py:67）
  → SSH direct-tcpip → 训练机 → 10.60.216.3:8001
  → GatewayActor（gateway.py:91 路由 → 162 处理）→ openai_to_internal
  → GatewaySession.run_generation（session.py:220）→ backend.generate（vLLM）
  → decode_response → 工具调用 → 腾讯沙箱执行 → 结果回传下一轮
```

- **该任务状态**：与内部形态 §5.7 相同的 6 轮节奏（读 buggy → heredoc 修复 →
  自测 → 提交），token 结构应一致（4 段生成 + 3 段工具结果，411 模型 token）。

### 3.7 创建 done 标记

- **位置**：本地 WSL → 训练机（SSH exec）。
- **代码**：`process_one_task`（243）：
  `client.exec_command(f"mkdir -p {remote_dir} && touch {done_marker}")`。
- **该任务状态**：**服务器出现 `<session_id>.done`**；训练侧 `_wait_for_done`
  （`external_agent_runner.py:77`）检测到后结束轮询。

### 3.8 云侧 reward + POST

- **位置**：腾讯沙箱（pytest）+ 训练机（POST 到 Gateway）。
- **代码**：`external_agent_runner.py:117-127`：
  `evaluate_reward_msa(sandbox, task, timeout=600)`（写 hidden test → pytest
  `test_solution.py::test_all`）→ `POST session.reward_info_url`，
  payload `{"reward": 1.0, "reward_score": 1.0, "resolved": True, ...}`。
- **该任务状态**：`1 passed in 0.01s` → reward=1.0 → Gateway session 记录。

### 3.9 finalize → TQ → trainer

- **位置**：训练机 node1（framework 主进程）。
- **代码**：`framework.py:700` `finalize_session`（`gateway.py:269` →
  `session.py:339`）→ 轨迹物化（turns=6 / prompt 1288 / response 1004 /
  reward 1.0）→ `_write_session_trajectories_to_tq`（878）→ TQ →
  trainer GRPO 更新；`external_agent_runner.py` finally 清理 `.task.json` 与
  `.task.json.claimed` + done，`sandbox.stop()`。
- **该任务状态**：正样本进 step 训练；本地侧 `process_one_task` 返回后删除本地
  claimed 引用并退出。

## 4. 每步文件系统状态（本任务视角）

| 步骤 | 服务器 `/home/ubuntu/swe-rl/platform_test/` | 本地 `/tmp/` | 沙箱 `/testbed/` |
|---|---|---|---|
| 3.0 | （空） | — | — |
| 3.1 | — | — | `solution.py`（buggy） |
| 3.2 | `<session>.task.json` | — | 同上 |
| 3.3 | `<session>.task.json.claimed`（认领） | 已读 payload | 同上 |
| 3.4-3.6 | `.claimed` | `platform_mini_swe_<id>.yaml` + traj.json | agent 修复后的 solution.py |
| 3.7 | `.claimed` + `<session>.done` | 同上 | 修复版 |
| 3.8-3.9 | 训练侧清理两种文件名 + done | 本地清理退出 | 沙箱 stop |

## 5. Gateway 轨迹处理（外部形态同内部形态）

轨迹物化路径与内部形态完全一致，见
`docs/任务剖析-内部形态.md` §6（路由 → run_generation → 链累积 → finalize →
Trajectory）与 §7（TQ 字段）。外部形态下 Gateway 仍在训练机 node1，`base_url`
经隧道访问，**物化的轨迹 token 级内容与内部形态相同**。

## 6. hang 点与并发（本任务视角）

- 本地没起：训练侧 `_wait_for_done` 等满 `run_timeout`（7200s）超时，仍会评估
  （沙箱未被 agent 修改 → pytest 大概率失败 → reward 0）；
- 认领后崩溃：`.claimed` 残留 → 其他实例在 mtime 超过 `--timeout` 后自动回收
  （`_recover_stale_claims`，`platform_local_agent.py:161`）重跑；
- 多实例：训练侧 `ROLLOUT_N/TRAIN_BATCH_SIZE/CONCURRENCY` 调大 → 并发写多个
  task.json；本地 N 个实例原子认领，互不冲突。

## 7. 外部形态关键点

- 任务交接 = 文件协议（task.json / done），agent 与训练框架完全解耦；
- 模型流量走 SSH 隧道，公网只出现在 SSH 连接（`UCLOUD1_HOST`）；
- 轨迹与 reward 全在云端（Gateway 物化 + 训练机 pytest），本地 agent 不碰训练
  数据——on-policy 保证与内部形态一致。
