# 任务级剖析：外部形态（平台本地 agent）同一条 reward=1 轨迹

> 用与「内部形态剖析」**同一条真实任务**（humanevalfix-Python-111，reward=1.0）
> 走外部协议（`external_agent_runner` + `platform_local_agent.py`）逐步推演。
> 说明：外部形态 P0 单步闭环已于 2026-08-12 验证（reward=1.0），但该次轨迹未
> 归档到 `work/logs`；本文任务内容、轨迹结构与内部形态一致（都由 Gateway 云侧
> 物化），差异只在“任务如何交接给 agent”。配套看
> `docs/平台本地agent流程.md`。

## 1. 任务本体（同内部形态）

humanevalfix-Python-111：修复 `histogram(test)`（空串 case 返回 `{'':1}` 的 bug），
hidden test `test_solution.py::test_all`，8 条断言。buggy 代码与修复方向见
`docs/任务剖析-内部形态.md` §1。

## 2. 与内部形态的差异总览

| 环节 | 内部形态 | 外部形态 |
|---|---|---|
| runner | `mini_swe_agent_runner`（训练机跑 agent） | `external_agent_runner`（不跑 agent）+ 本地 `platform_local_agent.py` |
| 任务交接 | 函数参数直传（同 Ray 集群） | **task.json 文件协议** |
| 完成信号 | runner 返回值 + reward POST | **done 标记文件** |
| agent 连模型 | 直连 `127.0.0.1:8001` | 本地 `127.0.0.1:<隧道端口>` → SSH 隧道 → Gateway |
| 沙箱 | 训练机侧建 + agent attach | 训练机侧建 + 本地 agent attach（同一实例） |
| reward | 训练机侧 evaluate | 训练机侧 evaluate（云侧） |
| 轨迹 | Gateway 物化 | Gateway 物化（一致） |

## 3. 逐步运行剖析（每步：代码位置 + 该任务此刻的状态）

### 步骤 0：训练侧启动（先起）

- 命令：`MODEL=/home/ubuntu/models/Qwen3-8B-final setsid nohup bash
  run_grpo_platform_test_ucloud.sh ... &`（加载已有权重，`save_freq=-1` 不保存）。
- 代码链：`main_ppo` → `AgentFrameworkRolloutAdapter.create`（起 GatewayManager +
  AgentFrameworkWorker）→ `fit` → 需要采样时调 `external_agent_runner`。
- 该任务状态：训练侧等待 sample 20 的采样请求；此时还没有任何任务文件。

### 步骤 1：external_agent_runner 建沙箱 + 注入 solution.py

- 代码：`uni_agent_ext/agents/external_agent_runner.py`（86 行）→
  `extract_task_meta`（拿 instance_id/FAIL_TO_PASS）→ `create_task_sandbox`
  → `sandbox.start()` → 沙箱内 `git init` + 写 buggy `solution.py` + `git add -A`。
- 该任务状态：腾讯 E2B 沙箱实例创建（如 `xxx...`），`/testbed/solution.py` 为
  buggy 实现；隐藏测试仍未注入（无泄露）。

### 步骤 2：写 task.json（任务下发）

- 代码：`_write_task_file`（47 行）→
  `<PLATFORM_TEST_DIR>/<session_id>.task.json`，字段（本任务实例值）：

```json
{
  "session_id": "session-sample-20-rollout-1-xxxx",
  "base_url": "http://10.60.216.3:8001/v1/sessions/xxx",   // Gateway 会话端点（内网）
  "instance_id": "<腾讯沙箱实例>",
  "image": "python:3.12",
  "raw_prompt": "<humanevalfix-Python-111 的 prompt 全文>",
  "tools_kwargs": {"env": {"files": {"solution.py": "<buggy 版>"}, ...}},
  "task": {"instance_id": "humanevalfix-Python-111", "issue": "<题目>"},
  "done_marker": "<PLATFORM_TEST_DIR>/<session_id>.done",
  "created_at": 1755...
}
```

- 该任务状态：训练侧进入 `_wait_for_done` 轮询（每 5s），本地侧此刻还看不到任何东西。

### 步骤 3：本地 agent 认领任务（后起）

- 命令（WSL）：`PYTHONPATH=$PWD/vendor/uni-agent:$PWD python
  scripts/platform/platform_local_agent.py --wait --timeout 1800`。
- 代码：`main` → paramiko 连训练机（`work/ucloud.env` 公网 IP）→
  `wait_for_task`（69 行）轮询 `*.task.json`，`sftp.rename` 原子认领成
  `*.claimed`（多实例时谁 rename 成功谁拥有）。
- 该任务状态：本地读到本任务的 task.json（session/base_url/instance_id/issue），
  打印 `task: session-sample-20-... instance=humanevalfix-Python-111`；训练侧
  继续等 done。

### 步骤 4：SSH 隧道

- 代码：`TunnelForwarder(transport, parsed.hostname, parsed.port)`（35 行）——
  direct-tcpip，本地 `127.0.0.1:<随机端口>` → 训练机内网 `10.60.216.3:8001`。
- 该任务状态：`tunnel up: 127.0.0.1:43210 -> 10.60.216.3:8001`；后续 agent 的
  模型请求走这条隧道。

### 步骤 5：生成 mini-swe config

- 代码：`build_config`（89 行）→ `build_mini_swe_config`：
  `base_url=http://127.0.0.1:43210<session path>`、`model=Qwen3-8B`、
  `max_turns=60`、`instance_id=<腾讯沙箱>`、`image=python:3.12`。
- 该任务状态：config 写入本地 `/tmp/platform_mini_swe_<session尾8位>.yaml`；
  agent 将 attach 云端已建沙箱。

### 步骤 6：本地跑 agent（6 轮）

- 代码：`run_local_agent`（114 行）→ `get_sb_environment` + `get_agent` →
  `agent.run(problem_statement)`，`ThreadPoolExecutor` + timeout 兜底。
- 该任务状态：与内部形态 §4 步骤 5 相同的 6 轮节奏（读文件 → heredoc 修复 →
  自测 → 提交），但每轮模型调用路径为：

```text
agent → http://127.0.0.1:43210（隧道）→ SSH → 训练机 → 10.60.216.3:8001
      → Gateway → vLLM 生成 → token 级轨迹由 Gateway 物化
工具调用 → 云端腾讯沙箱实例执行（同一 instance_id）
```

- token 结构应与内部形态一致（4 段生成 + 3 段工具结果，411 模型 token），因为
  模型、任务、沙箱内容相同，只换了网络路径。

### 步骤 7：创建 done 标记

- 代码：`client.exec_command(f"mkdir -p {remote_dir} && touch {done_marker}")`
  → 训练侧 `_wait_for_done` 检测到 `<session_id>.done` 存在。
- 该任务状态：训练侧 external_agent_runner 结束轮询，进入 reward 阶段。

### 步骤 8：云侧 reward + POST

- 代码：`evaluate_reward_msa(sandbox, task, timeout=600)`（写 hidden test →
  pytest `test_solution.py::test_all`）→ `POST session.reward_info_url`，
  payload `{"reward": 1.0, "reward_score": 1.0, "resolved": True, ...}`。
- 该任务状态：与内部形态相同——`1 passed in 0.01s`，reward=1.0。

### 步骤 9：finalize → TQ → trainer

- 代码：`gateway_manager.finalize_session` → 轨迹物化（turns=6 / prompt 1288 /
  response 1004 / reward 1.0）→ `_write_session_trajectories_to_tq` → TQ →
  trainer GRPO 更新。
- 该任务状态：同内部形态，正样本进 step 训练；本地侧处理完删除 claimed 文件。

## 4. 该任务在每步的文件系统状态

| 步骤 | 服务器 `<PLATFORM_TEST_DIR>/` | 本地 |
|---|---|---|
| 0-1 | （空） | — |
| 2 | `session-sample-20-rollout-1-xxx.task.json` | — |
| 3 | `...task.json` → `...task.json.claimed`（认领） | 已读 payload |
| 4-6 | `.claimed` 存在 | 隧道 + agent 运行 |
| 7 | `.claimed` + `...done`（已建） | 完成 |
| 8-9 | 训练侧清理两种文件名 + done，沙箱 stop | 删本地 claimed 引用，退出 |

## 5. 本任务的 hang 点与并发视角

- 若本地侧没起：训练侧 `_wait_for_done` 等满 `run_timeout`（7200s）超时，仍会
  评估（沙箱里没有 agent 改过文件 → pytest 大概率失败 → reward 0）；
- 若认领后崩溃：`.claimed` 残留，mtime 超过 `--timeout` 后被其他实例自动回收
  重跑（与内部失败重试语义一致）；
- 多实例：训练侧 `ROLLOUT_N/CONCURRENCY` 调大后并发写多个 task.json，本地 N 个
  实例各自原子认领，互不冲突。

## 6. 外部形态的关键点

- 任务交接 = 文件协议（task.json / done），agent 与训练框架完全解耦；
- 模型流量走 SSH 隧道，公网只出现在 SSH 连接（`UCLOUD1_HOST`）；
- 轨迹与 reward 全在云端（Gateway 物化 + 训练机 pytest），本地 agent 不碰训练
  数据——与内部形态的 on-policy 保证一致。
